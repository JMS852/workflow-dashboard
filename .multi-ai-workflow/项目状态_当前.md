# 项目状态：MQTT 任务流可视化交互系统

> 最后更新：2026-07-28 | 模式：standard | 阶段：Stage 1 — 骨架恢复

## 项目目标

构建一个独立的 Electron 桌面应用，用 MQTT 协议接收任务流，用可视化面板展示多 AI 并行执行过程，用户可以实时决策和批注。

## 技术栈

- **前端**: Electron 28 + React 18 + TypeScript 5 + Vite 5
- **后端引擎**: Python 3.12（独立 .venv）
- **AI 提供商**: DeepSeek / 通义千问 / 豆包 / 混元
- **通信协议**: MQTT（待实现）+ stdin/stdout JSON（已有）

## 模式: standard

- CC#1/CC#2 角色分离
- 阶段阀门必过
- 关键节点交叉审查
- Git 锚点标记

## 当前阶段: Stage 1 ✅ 骨架恢复

### 已完成
- [x] Git 初始化 + .gitignore
- [x] 从崩溃会话恢复 workflow-dashboard 代码
- [x] 合并 ai-executor 引擎到 engine/
- [x] 创建独立 Python venv（隔离 task-assistant）
- [x] 前端构建验证 (tsc + vite build)
- [x] Electron TS 编译验证
- [x] 初始 git commit + tag

### 当前数据契约

| 名称 | 格式 | 方向 | 说明 |
|---|---|---|---|
| Electron ↔ Python | stdin/stdout JSON | 双向 | 每行一个 JSON 对象 |
| 文件变更通知 | IPC (contextBridge) | Electron → React | chokidar 实时推送 |
| MQTT 任务流 | 待定义 | 外部 → App | v2 阶段实现 |

## 用户目标与优先级

- 优先级: 第一版做好，再搞 task-assistant
- 独立性: 二者完全独立，预留合并端口
- 合并协议: 标准化 JSON 协议，不硬编码

## 已通过阶段

- Stage 0: 崩溃恢复 ✅ (2026-07-28)

## 下一步

- Stage 2: MQTT 客户端模块 + 任务流可视化

## 隔离状态

| 项目 | 路径 | 状态 |
|---|---|---|
| workflow-dashboard | C:/Users/Lenovo/workflow-dashboard/ | 当前项目 ✅ |
| task-assistant | C:/Users/Lenovo/task-assistant/ | 已隔离 |
| Python venv | .venv/ (独立) | 已隔离 |

## CC#2 恢复字段

- 当前任务: 无待执行任务
- 相关文件: engine/**, src/**, electron/**
- 构建命令: `npm run build && npx tsc -p tsconfig.electron.json`
- 启动命令: `node launcher.js`
- 当前阻塞: 无
- 不应触碰: task-assistant 目录
